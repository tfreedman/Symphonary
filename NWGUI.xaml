<Window x:Class="NiceWindow.NWGUI"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="NiceWindow" Height="700" Width="1024" ResizeMode="NoResize">
    <Grid>
        <Menu IsMainMenu="True" Height="22px" VerticalAlignment="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_New..." />
                    <Separator />
                    <MenuItem Header="_Open..." />
                    <Separator />
                    <MenuItem Header="_Save" />
                    <MenuItem Header="_Save As..." />
                    <Separator />
                    <MenuItem Header="_Exit" />
                </MenuItem>
            <MenuItem Header="_Setup">
                <MenuItem Header="_New..." />
                <Separator />
                <MenuItem Header="_Open..." />
                <Separator />
                <MenuItem Header="_Save" />
                <MenuItem Header="_Save As..." />
                <Separator />
                <MenuItem Header="_Exit" />
            </MenuItem>
            <MenuItem Header="_Start" HorizontalAlignment="Right" Click="button1_Click"/>
            <MenuItem Header="_About" HorizontalAlignment="Right"/>
        </Menu>
        <Canvas Name="canv" Height="703" HorizontalAlignment="Left" VerticalAlignment="Bottom" Width="1002" />
    </Grid>
</Window>
